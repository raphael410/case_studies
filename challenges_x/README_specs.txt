For problem 1 & 2 :

You have two possibilities to run the script.

You need to use "jupyter notebook" to run the script.
Install jupyter notebook with "pip install jupyter" or "pip3 install jupyter" in the command prompt or with conda.
Once installed, run the following in the command prompt, in the working directory : "jupyter notebook"

Then follow the instructions in Software prerequisites.

Problem 1


Problem 2